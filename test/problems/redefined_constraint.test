# INPUT
con const(list, n: int) { all:
  size == n;
}

con test(MACHINE) { all: }

con test(MACHINE) { all: }

con main { all:
    const(1) test();
}

# OUTPUT
>> con test(MACHINE) { all: }
>>     ^^^^
fatal problem: already defined @ line 7
