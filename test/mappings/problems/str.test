# INPUT
map main {
  final a = str(range(0, 10));
}

# OUTPUT
test.col @ line 2
>>   final a = str(range(0, 10));
>>                 ^^^^^^^^^^^^
fatal problem: expression <list num> not compatible with <any>
