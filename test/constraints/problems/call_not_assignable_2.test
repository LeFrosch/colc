# INPUT
con const(list, n: str) { all:
  size == n;
}

con main { all:
    const(1 + 3
    + 5) MACHINE;
}

# OUTPUT
test.col @ line 6
>>     const(1 + 3...
>>           ^^^^^
fatal problem: cannot assign <int> to <str>
