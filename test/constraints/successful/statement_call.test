# INPUT
con const(list, n: int) { all:
  size == n;
}

con name(MACHINE) { all:
  os == "linux";
}

con main { any:
  const(3) name();
}

# OUTPUT
[ANY, [WITH, 'MACHINE', [ALL, [EQUAL, [LIST_SIZE], 3]], [ALL, [EQUAL, [ATTR, 'os'], 'linux']]]]
